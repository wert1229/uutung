<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.sist.erp.dao.TempDAO">
	<select id="searchProduct" resultType="com.sist.erp.vo.TempVO">
		SELECT * FROM PRODUCT WHERE NAME LIKE #{key} AND STATE='Y' ORDER BY TO_NUMBER(PSEQ) DESC
	</select>
	
	
	<select id="searchEstProduct" resultType="com.sist.erp.vo.EstiProductVO">
		SELECT P.PSEQ, P.NAME, P.CATEGORY, E.PRICE, P.NOTE
		FROM PRODUCT P, ESTIMATE E
		WHERE E.PRODUCT_SQ = P.PSEQ AND E.CLIENT_SQ=#{param2} AND P.NAME LIKE #{param1}
	</select>
	
	
	
	
</mapper>